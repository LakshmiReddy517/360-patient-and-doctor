package com.pcare.servicerequest.domain;

/** Where the patient is picked up from (blueprint point 9). */
public enum PickupSource {
    HOME,
    RAILWAY_STATION,
    AIRPORT,
    BUS_STATION,
    HOTEL,
    CURRENT_LOCATION,
    OTHER
}
